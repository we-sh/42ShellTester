This test check if you are using folders into the $PATH environement variable
We are changing the actual PATH by PATH=/tmp/virtualpath/p1:/tmp/virtualpath/p2
And executing the commande display_name located inside those folders
